import MarketPlace from '@/components/market-place-components/MarketPlace';
import React from 'react';

const MarketplaceManager: React.FC = () => {

  return (
   <section>
    <MarketPlace />
   </section>
  );
};

export default MarketplaceManager;